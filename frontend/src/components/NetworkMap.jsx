import React from 'react';
import { NetworkMap as NetworkMapTSX } from '../../../src/components/NetworkMap';

export const NetworkMap = (props) => {
  return <NetworkMapTSX {...props} />;
};

export default NetworkMap;
